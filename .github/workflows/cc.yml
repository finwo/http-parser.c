---

name: C/C++ CI

on:
  - push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Install @finwo/dep
        uses: finwo/dep@edge
      # - name: configure
      #   run: ./configure
      - name: Install dependencies
        run: dep i
      - name: make
        run: make
      - name: make check
        run: make check
# - name: make distcheck
#   run: make distcheck
